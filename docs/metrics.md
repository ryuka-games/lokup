# メトリクス仕様

Lokup が計算・表示するメトリクスの詳細仕様。

## 概要

| メトリクス | 説明 | 警告閾値 |
|-----------|------|----------|
| 開発効率スコア | 基本100点からリスクに応じて減点 | 60点未満 |
| コード健全性スコア | 基本100点からリスクに応じて減点 | 60点未満 |
| コミット/日 | 期間中の1日あたり平均コミット数 | - |
| 深夜コミット率 | 22:00〜翌5:00のコミット割合 | 30%以上 |
| PRリードタイム | PR作成からマージまでの平均日数 | 7日以上 |
| バグ修正割合 | マージ済みPRのうちバグ修正PRの割合 | 50%以上 |

---

## PR分類ロジック

PRはブランチ名のプレフィックスで分類される。

### 分類ルール

| 分類 | ブランチ名パターン | 例 |
|------|-------------------|-----|
| Feature | `feature/*`, `feat/*` | `feature/add-login`, `feat/new-api` |
| BugFix | `fix/*`, `bugfix/*`, `hotfix/*` | `fix/null-pointer`, `hotfix/security-patch` |
| Other | 上記以外 | `chore/update-deps`, `refactor/cleanup`, `main` |

### 判定の優先順位

1. `feature/` または `feat/` で始まる → **Feature**
2. `fix/`, `bugfix/`, `hotfix/` のいずれかで始まる → **BugFix**
3. それ以外 → **Other**

### バグ修正割合の計算

```
バグ修正割合(%) = BugFix件数 / (Feature + BugFix + Other) × 100
```

**注意**: マージ済みPRのみがカウント対象。

---

## 深夜コミット率

### 定義

深夜帯（22:00〜翌5:00）に作成されたコミットの割合。

### 計算式

```
深夜コミット率(%) = 深夜帯コミット数 / 総コミット数 × 100
```

### 深夜帯の判定

コミットの `Date` フィールドの時刻（hour）で判定:
- `hour >= 22` または `hour < 5` → 深夜コミット

---

## PRリードタイム

### 定義

PRが作成されてからマージされるまでの平均日数。

### 計算式

```
リードタイム(日) = Σ(マージ日時 - 作成日時) / マージ済みPR数
```

### 対象

- マージ済みPRのみ（`MergedAt != nil`）
- クローズされたがマージされなかったPRは除外

---

## スコア計算

### 開発効率スコア

基本100点から、検出されたリスクに応じて減点。

| リスク重大度 | 減点 |
|-------------|------|
| High (🔴) | -15点 |
| Medium (🟡) | -10点 |
| Low (🟢) | -5点 |

### コード健全性スコア

基本100点から、検出されたリスクに応じて減点。

| リスク重大度 | 減点 |
|-------------|------|
| High (🔴) | -10点 |
| Medium (🟡) | -5点 |
| Low (🟢) | -2点 |

### グレード

| スコア | グレード | 評価 |
|--------|---------|------|
| 80-100 | A | 良好 |
| 60-79 | B | 普通 |
| 40-59 | C | 要改善 |
| 0-39 | D | 危険 |

---

## リスク検出

### 変更集中リスク

1ファイルへの変更が集中している場合に検出。

| 条件 | 重大度 |
|------|--------|
| 30日間で20回以上変更 | High |
| 30日間で10回以上変更 | Medium |

### 属人化リスク

1人のコントリビューターがコミットの大部分を占める場合に検出。

| 条件 | 重大度 |
|------|--------|
| 1人が80%以上のコミット | Medium |

### 深夜労働リスク

深夜帯のコミットが多い場合に検出。

| 条件 | 重大度 |
|------|--------|
| 深夜コミット率30%以上 | Medium |

---

## 制限事項

- ブランチ命名規則に従っていないリポジトリでは、PR分類が正確に機能しない
- GitHub API のレート制限により、大規模リポジトリでは一部データが取得できない場合がある
- コミット日時はGitHub APIから取得したUTC時刻を使用
